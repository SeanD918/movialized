{
  "name": "movialized-root",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.2.1"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build"
  }
}
